<h2>Welcome to Cats</h2>
<center>
<a href="<%= new_cat_url %>">
<button type="button" class="btn btn-primary pull-right">Add cat</button>
</a>
</center>
<br>

<!-- <table class="table table-striped table-bordered " >
  <thead>
    <tr>
      <th scope="col">Name</th>
      <th scope="col">Age</th>
      <th scope="col">Color</th>
      <th scope="col">Edit</th>
      <th scope="col">Delete</th>
    </tr>
  </thead>
  <tbody>
  <% @cats.each do |cat| %>
    <tr>
      <td>
      <a href="<%= cat_url(cat) %>">
      <%= image_tag("cat.png") %>
      </a>
      </td>
      <td><%= cat.age %></td>
      <td><%= cat.color %></td>
        <td>
            <a href="<%= edit_cat_url(cat) %>">
            <button type="button" class="btn btn-warning">Edit</button>
            </a>
        </td>            
        <td>
            <form action="<%= cat_url(cat) %>" method="POST"> 
                <input type="hidden" name="_method" value="DELETE">
                <button type="submit" class="btn btn-danger">Delete</button>
            </form>
        </td>
    </tr>
    <% end %>
  </tbody>
</table> -->

<div class="container">
<div class="card-deck content-center">
  <% @cats.each do |cat| %>
    <div class="col-sm-3">
        <a href="<%= cat_url(cat) %>">
          <%= image_tag("cat.png") %>
        </a>
        <div class="card-body">
          <%= cat.name %>
        </div>
    </div>
  <% end %>
</div>
</div>